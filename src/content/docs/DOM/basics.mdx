---
title: DOM Basics
description: DOM Basics
sidebar:
  order: 2
---
import { YouTube } from 'astro-embed';

## DOM Minimal example

![basic html](/htmlcssjavascript/htmlminimal.png)

![basic html DOM](/htmlcssjavascript/htmlminimaldom.png)
## HTML Parsing
The browser starts at the top:

1. It sees &lt;html&gt;→ creates the root &lt;html&gt; node.
2. Inside it sees &lt;body> → makes a &lt;body&gt; node.
3. Inside &lt;body&gt;
    * &lt;h1&gt; → creates a heading node with text "Hello".
    * &lt;p&gt; → creates a paragraph node with text "World".
4. Each DOM node corresponds to a HTML element.
5. The DOM is hierarchical, like files and folders, there is a parent-child relationship between nodes.

## Javascript

```js title="index.js"
// change the paragraph from World to Everyone
let para = document.querySelector("p");
para.textContent = "Everyone";
```

Now javascript has changed the DOM

![Changed DOM](/htmlcssjavascript/htmldomchanged.png)

... so the webpage changes.

The first 80 seconds of this video summarises the points above.

<YouTube id="J-02VNxE7lE" />



## Walk the DOM tree yourself

[Live example of above](https://mohankumargupta.github.io/webcourse/minimalhelloworld/)

Right click->Inspect->Console.

```
console.dir(document);
```

Find the property on document called **childNodes** and expand and follow down the tree, then look at this image again.

![basic html DOM](/htmlcssjavascript/htmlminimaldom.png)

## Further Reading

